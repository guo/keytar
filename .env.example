# Keytar Configuration
# These environment variables are used by the keytar bin scripts

# Service name for keytar (required)
# This identifies your application in the system keychain
KEYTAR_SERVICE_NAME=my-app

# Salt key name for encryption (optional, defaults to 'salt-key')
# This is used to identify the salt value in the keychain
KEYTAR_SALT_KEY=salt-key
